modulo std.frontend.http

usa std.base.resultado

campo fetch_texto(url: Texto) -> Resultado<Texto, Falha> toca frontend, rede ::
  devolve frontend_fetch_texto(url)
fecha

campo fetch_json(url: Texto) -> Resultado<Texto, Falha> toca frontend, rede ::
  devolve frontend_fetch_texto(url)
fecha
