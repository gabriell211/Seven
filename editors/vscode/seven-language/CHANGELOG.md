# Changelog

## 0.2.1

- Bumps the VSIX package version for the Seven 0.2.1 distribution release.
- Keeps the extension syntax/configuration-only while the Windows language
  installer moves to the separate Seven MSI.

## 0.2.0

- Adds a complete VSCode extension manifest for `.sev` files.
- Ships Seven language registration, grammar contribution and language
  configuration as an installable extension package.
- Documents local Extension Development Host and VSIX packaging flow.

## 0.1.0

- Adds initial TextMate grammar and language configuration for Seven.
