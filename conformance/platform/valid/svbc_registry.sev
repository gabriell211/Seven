modulo conformance.platform.valid.svbc_registry

usa seven.runtime.platform.intrinsic

campo inicio() -> Num ::
  guarda itens := intrinsecos_padrao()

  veja lista_tamanho(itens) > 0 ::
    devolve 0
  outro ::
    devolve 1
  fecha
fecha
