# Conformidade Seven-0

Estes arquivos definem o minimo que o seed precisa aceitar ou rejeitar.

`valid/` deve compilar para `SVBC0`.

`invalid/` deve falhar com o codigo indicado no comentario `espera:`.
